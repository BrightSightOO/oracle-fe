/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import type { ResolvedAccount, ResolvedAccountsWithIndices } from "../shared";
import type { Bounds, BoundsArgs } from "../types";
import type { Context, Pda, PublicKey, TransactionBuilder } from "@metaplex-foundation/umi";
import type { Serializer } from "@metaplex-foundation/umi/serializers";

import { transactionBuilder } from "@metaplex-foundation/umi";
import { mapSerializer, struct, u8 } from "@metaplex-foundation/umi/serializers";

import { getAccountMetasAndSigners } from "../shared";
import { getBoundsSerializer } from "../types";

// Accounts.
export type UpdateCurrencyV1InstructionAccounts = {
  /** Config */
  config: PublicKey | Pda;
  /** Currency */
  currency: PublicKey | Pda;
  /** Oracle authority */
  authority?: PublicKey | Pda;
};

// Data.
export type UpdateCurrencyV1InstructionData = {
  discriminator: number;
  newRewardRange: Bounds;
  newBondRange: Bounds;
};

export type UpdateCurrencyV1InstructionDataArgs = {
  newRewardRange: BoundsArgs;
  newBondRange: BoundsArgs;
};

export function getUpdateCurrencyV1InstructionDataSerializer(): Serializer<
  UpdateCurrencyV1InstructionDataArgs,
  UpdateCurrencyV1InstructionData
> {
  return mapSerializer<UpdateCurrencyV1InstructionDataArgs, any, UpdateCurrencyV1InstructionData>(
    struct<UpdateCurrencyV1InstructionData>(
      [
        ["discriminator", u8()],
        ["newRewardRange", getBoundsSerializer()],
        ["newBondRange", getBoundsSerializer()],
      ],
      { description: "UpdateCurrencyV1InstructionData" },
    ),
    (value) => ({ ...value, discriminator: 5 }),
  );
}

// Args.
export type UpdateCurrencyV1InstructionArgs = UpdateCurrencyV1InstructionDataArgs;

// Instruction.
export function updateCurrencyV1(
  context: Pick<Context, "identity" | "programs">,
  input: UpdateCurrencyV1InstructionAccounts & UpdateCurrencyV1InstructionArgs,
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    "optimisticOracle",
    "DVM3hK9SDgXLmVoLng1KrTJCzTnhw31hAnqTYP7uGCot",
  );

  // Accounts.
  const resolvedAccounts = {
    config: {
      index: 0,
      isWritable: false as boolean,
      value: input.config ?? null,
    },
    currency: {
      index: 1,
      isWritable: true as boolean,
      value: input.currency ?? null,
    },
    authority: {
      index: 2,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: UpdateCurrencyV1InstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity.publicKey;
  }

  // Accounts in order.
  const orderedAccounts: Array<ResolvedAccount> = Object.values(resolvedAccounts).sort(
    (a, b) => a.index - b.index,
  );

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(orderedAccounts, "programId", programId);

  // Data.
  const data = getUpdateCurrencyV1InstructionDataSerializer().serialize(resolvedArgs);

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}

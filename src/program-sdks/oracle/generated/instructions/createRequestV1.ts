/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import type { ResolvedAccount, ResolvedAccountsWithIndices } from "../shared";
import type { RequestKind, RequestKindArgs } from "../types";
import type {
  Context,
  DateTime,
  DateTimeInput,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
} from "@metaplex-foundation/umi";
import type { Serializer } from "@metaplex-foundation/umi/serializers";

import { findAssociatedTokenPda } from "@metaplex-foundation/mpl-toolbox";
import { mapDateTimeSerializer, transactionBuilder } from "@metaplex-foundation/umi";
import {
  i64,
  mapSerializer,
  publicKey as publicKeySerializer,
  string,
  struct,
  u64,
  u8,
} from "@metaplex-foundation/umi/serializers";

import { findRewardPda } from "../../hooked";
import { findCurrencyV1Pda, findOracleV1Pda } from "../accounts";
import { expectPublicKey, expectSome, getAccountMetasAndSigners } from "../shared";
import { getRequestKindSerializer } from "../types";

// Accounts.
export type CreateRequestV1InstructionAccounts = {
  /** Oracle */
  oracle?: PublicKey | Pda;
  /** Config */
  config: PublicKey | Pda;
  /** Request */
  request: PublicKey | Pda;
  /** Reward currency */
  rewardCurrency?: PublicKey | Pda;
  /** Bond currency */
  bondCurrency?: PublicKey | Pda;
  /** Reward mint */
  rewardMint: PublicKey | Pda;
  /** Reward source token account */
  rewardSource?: PublicKey | Pda;
  /** Reward escrow token account */
  rewardEscrow?: PublicKey | Pda;
  /** Creator */
  creator?: Signer;
  /** Payer */
  payer?: Signer;
  /** SPL token program */
  tokenProgram?: PublicKey | Pda;
  /** System program */
  systemProgram?: PublicKey | Pda;
};

// Data.
export type CreateRequestV1InstructionData = {
  discriminator: number;
  reward: bigint;
  bond: bigint;
  timestamp: DateTime;
  arbitrator: PublicKey;
  kind: RequestKind;
  uri: string;
};

export type CreateRequestV1InstructionDataArgs = {
  reward: number | bigint;
  bond: number | bigint;
  timestamp: DateTimeInput;
  arbitrator: PublicKey;
  kind: RequestKindArgs;
  uri: string;
};

export function getCreateRequestV1InstructionDataSerializer(): Serializer<
  CreateRequestV1InstructionDataArgs,
  CreateRequestV1InstructionData
> {
  return mapSerializer<CreateRequestV1InstructionDataArgs, any, CreateRequestV1InstructionData>(
    struct<CreateRequestV1InstructionData>(
      [
        ["discriminator", u8()],
        ["reward", u64()],
        ["bond", u64()],
        ["timestamp", mapDateTimeSerializer(i64())],
        ["arbitrator", publicKeySerializer()],
        ["kind", getRequestKindSerializer()],
        ["uri", string()],
      ],
      { description: "CreateRequestV1InstructionData" },
    ),
    (value) => ({ ...value, discriminator: 6 }),
  );
}

// Extra Args.
export type CreateRequestV1InstructionExtraArgs = {
  /** Bond mint */
  bondMint: PublicKey;
};

// Args.
export type CreateRequestV1InstructionArgs = CreateRequestV1InstructionDataArgs &
  CreateRequestV1InstructionExtraArgs;

// Instruction.
export function createRequestV1(
  context: Pick<Context, "eddsa" | "identity" | "payer" | "programs">,
  input: CreateRequestV1InstructionAccounts & CreateRequestV1InstructionArgs,
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    "optimisticOracle",
    "DVM3hK9SDgXLmVoLng1KrTJCzTnhw31hAnqTYP7uGCot",
  );

  // Accounts.
  const resolvedAccounts = {
    oracle: {
      index: 0,
      isWritable: true as boolean,
      value: input.oracle ?? null,
    },
    config: {
      index: 1,
      isWritable: false as boolean,
      value: input.config ?? null,
    },
    request: {
      index: 2,
      isWritable: true as boolean,
      value: input.request ?? null,
    },
    rewardCurrency: {
      index: 3,
      isWritable: false as boolean,
      value: input.rewardCurrency ?? null,
    },
    bondCurrency: {
      index: 4,
      isWritable: false as boolean,
      value: input.bondCurrency ?? null,
    },
    rewardMint: {
      index: 5,
      isWritable: false as boolean,
      value: input.rewardMint ?? null,
    },
    rewardSource: {
      index: 6,
      isWritable: true as boolean,
      value: input.rewardSource ?? null,
    },
    rewardEscrow: {
      index: 7,
      isWritable: true as boolean,
      value: input.rewardEscrow ?? null,
    },
    creator: {
      index: 8,
      isWritable: false as boolean,
      value: input.creator ?? null,
    },
    payer: {
      index: 9,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
    tokenProgram: {
      index: 10,
      isWritable: false as boolean,
      value: input.tokenProgram ?? null,
    },
    systemProgram: {
      index: 11,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: CreateRequestV1InstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.oracle.value) {
    resolvedAccounts.oracle.value = findOracleV1Pda(context);
  }
  if (!resolvedAccounts.rewardCurrency.value) {
    resolvedAccounts.rewardCurrency.value = findCurrencyV1Pda(context, {
      config: expectPublicKey(resolvedAccounts.config.value),
      mint: expectPublicKey(resolvedAccounts.rewardMint.value),
    });
  }
  if (!resolvedAccounts.bondCurrency.value) {
    resolvedAccounts.bondCurrency.value = findCurrencyV1Pda(context, {
      config: expectPublicKey(resolvedAccounts.config.value),
      mint: expectSome(resolvedArgs.bondMint),
    });
  }
  if (!resolvedAccounts.creator.value) {
    resolvedAccounts.creator.value = context.identity;
  }
  if (!resolvedAccounts.rewardSource.value) {
    resolvedAccounts.rewardSource.value = findAssociatedTokenPda(context, {
      mint: expectPublicKey(resolvedAccounts.rewardMint.value),
      owner: expectPublicKey(resolvedAccounts.creator.value),
    });
  }
  if (!resolvedAccounts.rewardEscrow.value) {
    resolvedAccounts.rewardEscrow.value = findRewardPda(context, {
      request: expectPublicKey(resolvedAccounts.request.value),
    });
  }
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.tokenProgram.value) {
    resolvedAccounts.tokenProgram.value = context.programs.getPublicKey(
      "splToken",
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
    );
    resolvedAccounts.tokenProgram.isWritable = false;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      "splSystem",
      "11111111111111111111111111111111",
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: Array<ResolvedAccount> = Object.values(resolvedAccounts).sort(
    (a, b) => a.index - b.index,
  );

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(orderedAccounts, "programId", programId);

  // Data.
  const data = getCreateRequestV1InstructionDataSerializer().serialize(
    resolvedArgs as CreateRequestV1InstructionDataArgs,
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
